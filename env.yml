name: Env Valiable
on: push
jobs:
  run:
    runs-on: ubuntu-latest
    env:
      BRANCH: main
    steps:
      - run: echo "${BRANCH}" # シェルコマンドからジョブレベルの環境変数を参照
      - uses: actions/checkout@v4
        with:
          ref: ${{ env.BRANCH }}"}" # envコンテキストからジョブレベルの環境変数を参照
